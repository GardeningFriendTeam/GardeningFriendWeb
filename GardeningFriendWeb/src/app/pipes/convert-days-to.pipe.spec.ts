import { ConvertDaysToPipe } from './convert-days-to.pipe';

describe('ConvertDaysToPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDaysToPipe();
    expect(pipe).toBeTruthy();
  });
});
